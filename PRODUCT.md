# VoxLocal 产品介绍

## 产品定位

VoxLocal 是一个 Mac 优先、开箱即用、完全本地运行的 AI 文档朗读与声音克隆工具。用户可以录制自己的声音、对录音降噪优化、创建可重复使用的克隆音色，再把文本、PDF、Word、EPUB 或 Markdown 转换为完整音频。

它同时是一款桌面产品和可插拔语音基础设施：Codex、GPT、WorkBuddy、自动化 Agent 或任何 MCP 客户端都可以把生成的文档交给 VoxLocal，在用户设备上生成声音。

## 核心价值

1. **隐私优先**：文档、参考录音、模型推理和生成音频默认不离开设备。
2. **真实可用的本地声音**：支持公共音色、用户录制克隆音色和多语言轻量音色。
3. **长文可靠生成**：自动语义分片、逐片生成、状态持久化和 WAV 无缝拼接。
4. **面向 Agent 插拔**：桌面 UI、Rust 命令、MCP 工具和文件输出采用同一任务模型。
5. **设备自适应**：根据 Apple Silicon 统一内存自动选择模型量化版本和分片大小。

## 用户流程

### 创建声音

用户在“录制音色”页面读取随机参考文字。录音完成后可以试听、废弃、重新录制或执行本地降噪。保存后生成可持久恢复的克隆音色档案。

### 管理声音

音色库包含全部、公共、克隆三个 Tab。公共音色按需下载，试听文本支持随机与自定义。当前使用音色、置顶、喜欢、克隆音色和普通音色按权重排序，同一层级可拖拽调整。删除的克隆音色先进入回收站。

### 阅读与生成

用户可以粘贴文本、导入文档或从文档库打开内容。生成与播放互相独立：生成任务完成后写入本地 WAV；播放器直接读取已有文件，不会因为点击播放而重新推理。

### 管理历史

“生成历史”只展示用户主动点击生成的完整音频，支持播放、停止、改名、Finder 定位和移入回收站。默认试听、系统资源和缓存进入“系统音频”分类，不污染用户历史。

### 外挂调用

Agent 通过 MCP 可以检查健康状态、列出音色、提交文本或文档、查询任务进度并获取输出路径。模型与桌面端共用同一套本地缓存，避免重复下载。

## 音色体系

- 9 个 Qwen3-TTS CustomVoice 高质量预设
- 54 个 Kokoro 82M 轻量多语言音色
- 用户本人或已获授权声音创建的克隆音色

公共声音包有独立下载状态和缓存。试听采用 `音色 ID + 文本` 内容寻址；相同音色朗读相同文本时直接复用 WAV。

## 产品边界

- 当前正式推理目标是 Apple Silicon Mac。
- 声音克隆仅用于本人声音或已获得明确授权的声音。
- VoxLocal 不提供云端账户、云同步或远程声音托管。
- 安装包在功能验收完成后构建，源码开发阶段使用 Tauri Dev。

## 后续方向

- 安装包签名、公证和自动更新
- 多任务队列的暂停、恢复与优先级
- 文档段落级时间轴与断点续听
- 更多本地模型适配器与跨设备任务调度
- 可选的局域网 Agent 网关与权限控制
